const PI = 3.141593

// This will throw an error
PI = 12

const person = {}

// This will not because the reference to the original object is retained
person.name = 'Renzel'

// var is replaced with let
let x = 10
